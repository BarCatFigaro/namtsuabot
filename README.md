# Namtsuabot; a discord bot with cool features eventually

Required to run:
    config.json in the form

        {
            "GoogleKey": <GOOGLE API KEY HERE>
            "DiscordToken": <DISCORD TOKEN HERE>
        }

To run:
    1. go build
    2. start the .exe